module Pa1309ArrayArityUnencodable = structure {
  pub let k : u64 = 3
  // Issue #1309 negative: `k` is not an encodable constant, so the packing
  // loop used to drop it and emit a 24-byte symbol for a [u64; 4] type.
  pub let mut mixed_array : [u64; 4] = [1, 2, k, 4]
}

// Phase 4 m11-004: hashmap_contains_key_false
// Fixture: HashMap::contains_key() returns false for a missing key.

use paideia.raw_mem;

fn test_hashmap_contains_key_false() -> bool {
  let hm = hashmap_new();
  hashmap_contains_key(hm, 42)
}

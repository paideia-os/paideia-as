module EnumLitCallArgFirstPos = structure {
  enum Choice { A, B }

  let helper : (Choice, u64) -> u64 = fn (c: Choice, v: u64) -> v

  pub let f : () -> u64 = fn () -> helper(A, 0x37)

  pub let entry : () -> u64 = fn () -> f()
}

// Phase 4 m11-002: vec_len_returns_count
// Fixture: Vec::len() returns element count.

use paideia.raw_mem;

fn test_vec_len_returns_count() -> u64 {
  let v = vec_new();
  let v = vec_push(v, 1);
  let v = vec_push(v, 2);
  vec_len(v)
}

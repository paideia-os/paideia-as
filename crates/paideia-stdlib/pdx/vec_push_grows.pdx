// Phase 4 m11-002: vec_push_grows
// Fixture: Vec::push(value) appends and grows.

use paideia.raw_mem;

fn test_vec_push_grows() -> u64 {
  let v = vec_new();
  let v = vec_push(v, 42);
  vec_len(v)
}

module Pa8ArrayLiteralData = structure {
  let arr : [u64; 8] = [1, 2, 3, 4, 5, 6, 7, 8]

  let _start : () -> () !{} @{} = fn(_: ()) -> unsafe {
    effects: {},
    capabilities: {},
    justification: "array literal data test",
    block: {
      mov rax, rbx
    }
  }
}

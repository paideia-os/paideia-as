module UartSmoke = structure {
  let _start : () -> () !{} @{} = fn(_: ()) -> unsafe {
    effects: {},
    capabilities: {},
    justification: "smoke test",
    block: {
      mov rax, rbx
    }
  }
}

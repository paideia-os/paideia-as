// Phase 4 m11-001: Result<T, E> fixture — Err construction.
//
// Phase 4 minimum: demonstrates enum-variant construction for Result<T, E>.
// Parser does not yet support generic type params in definitions (m9 gate).

trait ResultErrConstruct {
  fn result_err_construct() -> i32;
}

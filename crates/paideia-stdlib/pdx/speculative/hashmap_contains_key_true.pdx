// Phase 4 m11-004: hashmap_contains_key_true
// Fixture: HashMap::contains_key() returns true for a present key.

use paideia.raw_mem;

fn test_hashmap_contains_key_true() -> bool {
  let hm = hashmap_new();
  let hm = hashmap_insert(hm, 42, 100);
  hashmap_contains_key(hm, 42)
}

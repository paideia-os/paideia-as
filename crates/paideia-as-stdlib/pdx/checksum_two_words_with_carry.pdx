// Fixture: checksum_two_words_with_carry
// Tests ipv4_checksum with two 16-bit words with end-around carry.

trait TwoWordsWithCarryChecksum {
  fn ipv4_checksum(hdr: u64, len: u64) -> u16 !{RawMem} @{paideia.raw_mem};
}

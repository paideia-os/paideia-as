// v0.18 #998: Modified stub — str_chars_yields.
//
// Verify that str_chars iterates over each byte in a Str.
// Phase-4-m11-003 minimum: parse-only. Updated to align on Str { ptr: *u8, len: u64 } (v0.18 #998).
// Implementation blocked on #998c (iterator protocol).

struct Str {
  ptr: *u8,
  len: u64,
}

trait StrChars {
  fn str_chars(s: Str) -> Iterator<u8>;
}

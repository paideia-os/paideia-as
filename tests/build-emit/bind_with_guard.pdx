// Issue #1002: bind_with_guard
// Tests bind-and-match with guard that passes
// Expected exit code: 5

module BindWithGuard = structure {
  pub let entry : () -> u64 !{} @{} = fn(_ : ()) -> {
    match 5u64 {
      x @ 5u64 if x > 3u64 => x
      _ => 0u64
    }
  }
}

// Phase 4 m11-001: Option<T> fixture — map transformation.
//
// Phase 4 minimum: demonstrates map behavior for Option<T>.
// Parser does not yet support generic type params in definitions (m9 gate).

trait OptionMap {
  fn option_map() -> i32;
}

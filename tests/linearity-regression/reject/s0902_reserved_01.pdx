// Simple let shadow: a linear binding is shadowed without being consumed.
module M = structure {
  let f : (linear int) -> unit = fn (x : linear int) -> {
    let x = ();  // shadows the linear x; leaks it
    ()
  }
}

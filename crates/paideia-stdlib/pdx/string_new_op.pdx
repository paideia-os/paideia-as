// Phase 4 m11-003: Fixture — string_new_op
//
// Verify that string_new constructs an empty String.
// Phase-4-m11-003 minimum: parse-only; full elaborator-side execution
// gates on m1 walker work.

struct String {
  data: *u8,
  len: u64,
  cap: u64,
}

trait StringNew {
  fn string_new() -> String;
}

// v0.18 #998: Modified stub — str_bytes_yields.
//
// Verify that str_bytes iterates over the bytes of a Str.
// Phase-4-m11-003 minimum: parse-only. Updated to align on Str { ptr: *u8, len: u64 } (v0.18 #998).
// Implementation blocked on #998c (iterator protocol).

struct Str {
  ptr: *u8,
  len: u64,
}

trait StrBytes {
  fn str_bytes(s: Str) -> Iterator<u8>;
}

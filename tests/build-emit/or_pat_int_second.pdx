// Issue #1001: or_pat_int_second
// Tests or-pattern matching with integer literals (second alt matches)
// Expected exit code: 10

module OrPatIntSecond = structure {
  pub let entry : () -> u64 !{} @{} = fn(_ : ()) -> {
    match 1u64 {
      0u64 | 1u64 => 10u64
      _ => 20u64
    }
  }
}

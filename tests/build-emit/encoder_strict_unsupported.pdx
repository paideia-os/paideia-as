module EncoderStrictUnsupported = structure {
  let _start : () -> () !{} @{} = fn(_: ()) -> unsafe {
    effects: {},
    capabilities: {},
    justification: "encoder strict test - trigger encoder failure",
    block: {
      jmp does_not_exist_label
    }
  }
}

module MovMemImmSibDisp = structure {
  pub let f: () -> () !{} @{} = fn() -> unsafe {
    effects: {}, capabilities: {}, justification: "mov [rax + rcx*8 + 16], 42",
    block: {
      mov rax, 0
      mov rcx, 1
      mov [rax + rcx*8 + 16], 42
    }
  }
}

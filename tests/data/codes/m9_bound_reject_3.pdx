module M9BoundReject3 = structure {
  // Concrete impl exists for u64:Clone but not for i32:Clone, and we try to use i32 with Clone
  impl Clone for u64 {
    let clone : (u64) -> u64 = fn (x : u64) -> { x }
  }

  let use_clone_on_i32 : (i32) -> i32 =
    fn (x : i32) -> { x }
}

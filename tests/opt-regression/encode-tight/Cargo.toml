[package]
name = "paideia-opt-encode-tight"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

# This crate is *only* a test harness — it contains no library code or
# binaries, just the integration tests in `tests/runner.rs` that walk
# the corpus directory and validate REX/EVEX prefix tightening diagnostics
# (O-category: O1506).

[lib]
path = "src/lib.rs"

[dependencies]

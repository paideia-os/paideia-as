// Phase 4 m8-003: Fixture — string_from_str_coerces
//
// Verify that string_from_str coerces a &str literal to a String.
// Phase-4-m8-003 minimum: parse-only; full elaborator-side execution
// gates on m1 walker work.

struct String {
  data: *u8,
  len: u64,
  cap: u64,
}

trait StringFromStr {
  fn string_from_str() -> String;
}

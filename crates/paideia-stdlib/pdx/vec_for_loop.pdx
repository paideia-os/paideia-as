// Phase 4 m11-002: vec_for_loop
// Fixture: for x in v { ... } iteration pattern.

use paideia.raw_mem;

fn test_vec_for_loop() -> u64 {
  let v = vec_new();
  let v = vec_push(v, 1);
  let v = vec_push(v, 2);
  0
}

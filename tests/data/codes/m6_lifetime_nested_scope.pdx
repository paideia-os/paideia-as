; Nested scope — inner scope within outer scope.
; Should accept — outer binding's lifetime encompasses inner borrow.
fn test_nested_scope() -> u64 {
  let x: u64 = 40;
  {
    let r: &x = &x;
    let y: u64 = *r;
    y
  } + 2
}

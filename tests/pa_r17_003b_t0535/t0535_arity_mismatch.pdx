// PA-r17-003b (#1038): T0535 test - arity mismatch
//
// Fixture: Function pointer with mismatched arity.
// Lambda expects no parameters, but binding is typed (u64) -> u64.
// Expected: T0535 diagnostic (arity mismatch).

module T0535ArityMismatch = structure {
  let no_args : () -> u64 = fn() -> 42
  pub let mismatched : (u64) -> u64 = &no_args
}

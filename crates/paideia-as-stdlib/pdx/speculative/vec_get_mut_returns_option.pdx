// Phase 4 m11-002: vec_get_mut_returns_option
// Fixture: Vec::get_mut(idx) returns Option<&mut T>.

use paideia.raw_mem;

fn test_vec_get_mut_returns_option() -> u64 {
  let v = vec_new();
  let v = vec_push(v, 100);
  0
}

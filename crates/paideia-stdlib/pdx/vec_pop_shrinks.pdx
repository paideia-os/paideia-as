// Phase 4 m11-002: vec_pop_shrinks
// Fixture: Vec::pop() removes and shrinks.

use paideia.raw_mem;

fn test_vec_pop_shrinks() -> u64 {
  let v = vec_new();
  let v = vec_push(v, 42);
  let v = vec_pop(v);
  vec_len(v)
}

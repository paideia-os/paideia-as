; Phase 6 m4-006: Compound capability verification hot path (simplified)
;
; Architecture: unsafe block with multiple comparison instructions
; Target instructions (hot path):
;   cmp rax, rbx       => 48 39 D8  (3 bytes: REX.W + 0x39 + ModRM)
;   jne error          => (deferred to m4-007+)
;   cmp rcx, rdx       => 48 39 D1  (3 bytes: REX.W + 0x39 + ModRM)
;   jne error          => (deferred to m4-007+)
;   ret                => C3        (1 byte)
;   error: ...
;
; Current implementation: CMP + CMP + RET without conditional branches
; Full hot path with conditional verification deferred to Phase 6 m4-008+
; when label infrastructure proves stable across nested conditions.
;
; Expected total: 6 bytes
;
; Emitted bytes (Phase 6 m4-006):
48 39 D8 48 39 D1
;
; Acceptance: Multiple CMP instructions encode correctly without label infrastructure.
; Demonstrates compound control-flow sequence structure.
;

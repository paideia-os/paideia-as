; Mutable borrow followed by immutable borrow of the same binding.
; Should reject with S0906 — immutable cannot coexist with mutable.
fn test_mut_then_immut() -> u64 {
  let x: u64 = 50;
  let mref: &mut x = &mut x;
  let iref: &x = &x;
  *mref + *iref
}

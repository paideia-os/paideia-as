// Issue #1002: bind_or_pattern
// Tests bind-and-match with or-patterns
// Expected exit code: 11

module BindOrPattern = structure {
  pub let entry : () -> u64 !{} @{} = fn(_ : ()) -> {
    match 1u64 {
      x @ 0u64 | 1u64 | 2u64 => x + 10u64
      _ => 0u64
    }
  }
}

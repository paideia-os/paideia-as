; Inner scope borrow returned — borrow from inner scope returned to outer.
; Should reject with S0908 — inner scope region does not outlive outer return region.
fn test_inner_borrow_returned() -> &u64 {
  let x: u64 = 42;
  let r: &x = &x;
  r
}

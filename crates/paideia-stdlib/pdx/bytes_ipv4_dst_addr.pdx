// Fixture: bytes_ipv4_dst_addr
// Integration example: extract IPv4 destination address (bytes 16-19, big-endian).

trait IPv4DstAddr {
  fn get_u32_be(buf: u64, off: u64) -> u32 !{RawMem} @{paideia.raw_mem};
}

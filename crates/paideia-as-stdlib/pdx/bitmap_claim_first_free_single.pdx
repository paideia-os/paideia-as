// PA-R16-010 fixture: BitmapClaimFirstFreeSingle isolates bitmap_claim_first_free for parse regression.

trait BitmapClaimFirstFreeSingle {
  fn bitmap_claim_first_free(bmap: *u64, bit_capacity: u64) -> u64 !{Atomic};
}

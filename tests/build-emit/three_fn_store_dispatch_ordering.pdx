module ThreeFnStoreDispatchOrdering = structure {
    let mut a : u64 = 0
    let mut b : u64 = 0
    let mut c : u64 = 0
    pub let fa : (u64) -> u64 = fn (v: u64) -> a = v
    pub let fb : (u64) -> u64 = fn (v: u64) -> b = v
    pub let fc : (u64) -> u64 = fn (v: u64) -> c = v
}

module UefiStubWithData = structure {
  pub let payload : [u8; 8] = @include_bytes("data/include_bytes_probe.bin")
  pub let efi_main : (u64, u64) -> u64 = fn(image : u64, sys : u64) -> image @abi("ms")
}

module PaR12004UnsafeIfOk = structure {
  let DENIED : u64 = 0
  pub let request : (u64) -> u64 !{} @{} = fn (r: u64) -> unsafe {
    effects: {},
    capabilities: {},
    justification: "post-#913: if lives inside unsafe block; sanity fixture",
    block: {
      mov rax, rdi;
      ret
    }
  }
}

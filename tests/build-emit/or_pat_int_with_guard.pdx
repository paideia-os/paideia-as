// Issue #1001: or_pat_int_with_guard
// Tests or-pattern matching with guard expression
// Expected exit code: 10

module OrPatIntWithGuard = structure {
  pub let entry : () -> u64 !{} @{} = fn(_ : ()) -> {
    match 1u64 {
      0u64 | 1u64 if true => 10u64
      _ => 20u64
    }
  }
}

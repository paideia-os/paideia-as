// Fixture: bytes_get_u16_le
// Tests that get_u16_le(buf, off) reads a 16-bit little-endian value from a raw byte buffer.

trait GetU16Le {
  fn get_u16_le(buf: u64, off: u64) -> u16 !{RawMem} @{paideia.raw_mem};
}

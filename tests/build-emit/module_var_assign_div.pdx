// #1200 canary: division in module-level let-mut assignment
// Pattern: counter = 42u64 / 6u64; return counter
// Expected: 7

module ModuleVarAssignDiv = structure {
  let mut counter : u64 = 0u64

  pub let entry : () -> u64 =
    fn () -> {
      counter = 42u64 / 6u64;
      counter
    }
}

effect Io = {
  op print(x: u32)
}

effect Net = {
  op send(data: u32)
}

module M = structure {
  let broad_fn = fn _ !{Io, Net} -> perform Io::print(42); perform Net::send(1)

  let limited_caller = fn _ -> broad_fn()
}

// Phase 4 m8-003: Fixture — string_new_creates_empty
//
// Verify that string_new constructs a String with zero length and capacity.
// Phase-4-m8-003 minimum: parse-only; full elaborator-side execution
// gates on m1 walker work.

struct String {
  data: *u8,
  len: u64,
  cap: u64,
}

trait StringNew {
  fn string_new() -> String;
}

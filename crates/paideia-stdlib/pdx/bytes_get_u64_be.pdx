// Fixture: bytes_get_u64_be
// Tests that get_u64_be(buf, off) reads a 64-bit big-endian value from a raw byte buffer.

trait GetU64Be {
  fn get_u64_be(buf: u64, off: u64) -> u64 !{RawMem} @{paideia.raw_mem};
}

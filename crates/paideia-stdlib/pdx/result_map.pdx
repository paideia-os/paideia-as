// Phase 4 m11-001: Result<T, E> fixture — map transformation.
//
// Phase 4 minimum: demonstrates map behavior for Result<T, E>.
// Parser does not yet support generic type params in definitions (m9 gate).

trait ResultMap {
  fn result_map() -> i32;
}
